import Donation from '../components/DonationForm';
import '../styles.css'; 

const Stripe = () => {
    return (
        <div className="app-container">
            <Donation />
        </div>
    );
};

export default Stripe;
